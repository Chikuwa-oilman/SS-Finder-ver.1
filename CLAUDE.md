# プロジェクト指示書（Claude Code 用）

> このファイルは Claude Code がプロジェクトを開いたとき自動で読み込まれます。
> 全ての指示はここを最優先で守ってください。

## このプロジェクトについて

ガソリンスタンド検索アプリ「SS Finder」のクリーンルーム再構築プロジェクト（v2）。
詳細仕様は `REQUIREMENTS.md` を参照。

## 重要なルール

### 1. 既存ディレクトリには触らない
- 以下は **絶対に読まない・参照しない・コピーしない**：
  - `web/` （v1の本体）
  - `docs/` （v1のGitHub Pages公開コピー、`web/`と同内容）
  - `web_v2/` （放置中の改造、無視）
  - `server/` （今回スコープ外）
  - `doc/` （古いメモ類、無視）
- v2 の作業ディレクトリは新規作成する **`web_v3/`**。
- 比較・互換性のため v1 はすべて残しておく。

### 2. 言語と記法
- 出力・コメント・コミットメッセージはすべて **日本語**。
- コードのコメントは「なぜそうしたか」を書く（「何をしているか」はコードを読めば分かる）。

### 3. 技術制約
- **Vanilla HTML / CSS / JavaScript のみ**。
- フレームワーク（React, Vue 等）、バンドラ（Vite, webpack 等）、TypeScript は **使用禁止**。
- 外部ライブラリは原則なし。必要なら事前に提案して承認を得る。
- ESM (`<script type="module">`) は使ってよい。

### 4. ガードレール
- ファイルを 1 つ作るごと、または機能単位で `git commit` を提案する。
- 既存ファイルの削除や大規模な書き換えは、事前に diff を見せて承認を取る。
- API キーの実値や個人情報をコード・ドキュメントに書かない。
- 1 ファイルが 200 行を超えそうなら分割を提案する。

### 5. 進め方
- いきなり全部実装しない。**段階的に**。
  1. ファイル骨格（空のファイル群）を作って構成を確認
  2. UI（HTML + CSS）の静的な見た目
  3. APIキー設定フロー
  4. 検索 → Place Details 取得 → 表示
  5. 候補一覧と比較枠
  6. 検索履歴
  7. お気に入り
- 各段階の完了時に「次に進んでよいか」を確認する。

### 6. テスト・検証
- 自動テストは必須ではない（個人プロジェクト想定）。
- ただし JS は `eslint` の基本ルールでエラーが出ないこと。
- 機能ごとに、ブラウザで手動確認する手順を簡単に示すこと。

## ファイル構成

```
web_v3/
  index.html
  styles.css
  app.js              # コントローラ（薄く）
  config.example.js
  config.js           # .gitignore 済み（コミット禁止）
  lib/
    api.js            # Google Maps API ラッパー
    storage.js        # LocalStorage 操作
    ui.js             # DOM ヘルパー
README.md
.gitignore
```

## デザイン

- ダーク基調 + 緑系アクセント
- CSS変数で色を管理
- レスポンシブ（スマホ縦 → 1列、PC → グリッド）

## コミット規約

```
feat: 検索フォームを実装
fix: 候補一覧で重複が出るバグを修正
refactor: storage.js にお気に入り処理を分離
docs: README に開発手順を追記
```

## 困ったとき

- 仕様の解釈で迷ったら、勝手に判断せず質問する。
- Google Maps API の仕様（特にPlaces API New / Legacy の違い）で迷ったら、最新ドキュメントを確認すると伝える。
