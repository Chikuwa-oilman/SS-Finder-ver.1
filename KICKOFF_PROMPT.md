# Claude Code に最初に送る指示文

下のブロック全体をコピーして、VSCode の Claude Code チャットに貼り付けてください。

---

```
こんにちは。これからガソリンスタンド検索アプリ「SS Finder」を
クリーンルーム再構築します。

# やってほしいこと
1. まずプロジェクトルートの `CLAUDE.md` と `REQUIREMENTS.md` を読んでください。
2. 既存の `web/` `docs/` `web_v2/` `server/` `doc/` フォルダは
   絶対に読まないでください。`web/`と`docs/`はv1の本体と公開コピーで、
   比較対象として温存します。`web_v2/`は放置中の別件です。
3. 仕様の確認で気になった点を3〜5個、先に質問してください。
   （たとえば: Places API は New か Legacy か、ESM の `import` を使ってよいか、など）
4. 質問への回答後、以下の順で段階的に作業を進めてください。
   各段階の完了時に、私の承認を待ってから次に進んでください。

# 段階
- Step 1: `web_v3/` 配下のファイル骨格と `.gitignore` を作る
- Step 2: `index.html` と `styles.css` で UI の見た目だけ完成させる
- Step 3: APIキー設定フロー（保存・読み込み・クリア）
- Step 4: 検索 → Place Details 取得 → 基本情報・地図・SV 表示
- Step 5: 候補一覧と比較枠（最大3件）
- Step 6: 検索履歴（LocalStorage、最大20件）
- Step 7: お気に入り（追加・削除・JSON エクスポート/インポート）
- Step 8: README.md と動作確認手順

# 守ってほしいこと
- Vanilla HTML/CSS/JS のみ。フレームワーク・バンドラ・TS 禁止。
- 各 Step が終わるたびに `git commit` を提案。
- 1 ファイル 200 行を超えそうなら分割提案。
- 出力・コメント・コミットメッセージは日本語で。

それではまず Step 0 として、CLAUDE.md と REQUIREMENTS.md を読んだ上で
質問を投げてください。
```

---

## 補足：このプロンプトの意図

- **最初に質問させる**：Claudeに「分かったつもりで暴走」させないための仕掛け。Places API New/Legacy のような実装に効く論点を先に潰せる。
- **段階を明示**：各ステップが小さくレビュー可能なサイズに切られている。
- **承認待ちを明示**：勝手に8ステップ全部やられて差分が巨大化するのを防ぐ。
- **`docs/` を読まない**指示を二重に強調：v1 の癖（バグや構造の歪み）を引き継がせないため。

## 2回目以降のプロンプト例

各 Step 完了後に進めるとき：

```
Step 2 を確認しました。スマホ表示でフォームが少し詰まって
見えるので、検索ボタンの上にマージンを 8px 追加してください。
それが終わったら Step 3 に進んでください。
```

ハマったとき：

```
Place Details の API レスポンスから brand と operator が
取れません。期待していたフィールド名と違うかもしれないので、
実際のレスポンス構造を console.log で確認するコードを
一時的に挿入して、結果を見てから修正方針を提案してください。
```
