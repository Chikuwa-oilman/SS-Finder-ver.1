# SS Finder v2 要件仕様書

ガソリンスタンド検索アプリ「SS Finder」のクリーンルーム再構築版。
1年前にChatGPTで作った v1（`web/` と `docs/`）を参考にせず、本仕様書だけを根拠に新規実装する。
作業ディレクトリは `web_v3/`（`web_v2/` は別件で放置中のため番号を進める）。

---

## 1. 目的

店舗名（一部でも可）を入力すると、Google Maps Places API から該当ガソリンスタンドの基本情報・地図・ストリートビューを取得し、最大3件まで横並びで比較できるWebアプリ。社内・個人利用想定。

## 2. 非機能要件（先に決める）

- **技術スタック**: Vanilla HTML / CSS / JavaScript のみ。フレームワーク・バンドラ・トランスパイラ不可。
- **配信**: 静的ファイル（GitHub Pages でそのまま動くこと）。
- **対応ブラウザ**: 直近のChrome / Edge / Safari。
- **アクセシビリティ**: `aria-live` で結果領域を読み上げ、フォームラベルを正しく紐付け。
- **テーマ**: ダーク基調 + アクセントカラー（v1の配色を踏襲：背景 `#0f172a`、アクセント `#22c55e`）。
- **レスポンシブ**: スマホ縦持ちで1列、PCで複数列のグリッド。

## 3. APIキー管理

優先順位（高い順）でキーを取得する：

1. URLパラメータ `?gmaps_key=...`（あれば LocalStorage に保存）
2. LocalStorage `GMAPS_API_KEY`
3. `config.js` の `window.CONFIG.GOOGLE_MAPS_API_KEY`

未設定時は検索ボタンを `disabled` にし、画面上部に「APIキー設定」セクションを開く。`config.example.js` をリポジトリに置き、`config.js` は `.gitignore` 対象。

## 4. 画面構成

### 4.1 ヘッダー
- タイトル「SS Finder」
- 右側に「APIキー設定」ボタン

### 4.2 検索フォーム
- ラベル付きテキスト入力（例: `ENEOS 丸の内`）
- 「検索」ボタン
- 進行中は「キャンセル」ボタン表示
- ヒント文と、結果メッセージ領域（`info` / `success` / `warn` / `error` の4状態）

### 4.3 基本情報カード
取得して表示する項目：
- 名前 / 住所 / 電話 / 営業時間 / 営業状況 / ブランド / 運営会社 / 定休日 / 評価 / 公式サイト / Google地図リンク

### 4.4 地図カード
- Google Maps Embed（`iframe`、`loading="lazy"`、`referrerpolicy="no-referrer-when-downgrade"`）

### 4.5 ストリートビューカード
- Google Street View Embed（同上）

### 4.6 候補一覧
- 検索結果が複数ある場合、上位候補をカード表示。残りは名前のみ列挙。
- 各候補に「比較に追加」ボタン。

### 4.7 比較枠（最大3件）
- 「比較に追加」したものを横並びで表示。
- 各カードに「× 閉じる」ボタン。

## 5. 新機能（v2で追加）

### 5.1 検索履歴
- LocalStorage に直近20件を保存（クエリ + タイムスタンプ + ヒットしたPlace ID）。
- 検索ボックス下に「最近の検索」を折り畳みで表示、クリックで再検索。
- 「履歴を消す」ボタン。

### 5.2 お気に入り
- 基本情報カードに ⭐ ボタン。トグルでLocalStorageに保存（Place ID基準）。
- 「お気に入り一覧」セクション（折り畳み）から再検索可能。
- お気に入りはエクスポート/インポートできる（JSON、コピー&貼り付けで十分）。

## 6. データフロー

```
[入力] → Places Text Search → 候補配列
        → 1位（または選択候補）の Place Details 取得
        → 基本情報カード / 地図 / ストリートビュー を更新
        → 履歴に追加
```

## 7. ファイル構成（提案）

```
web_v3/
  index.html
  styles.css
  app.js
  config.example.js
  config.js          # .gitignore対象（コミット禁止）
  lib/
    api.js           # Google Maps API ラッパー
    storage.js       # LocalStorage（履歴・お気に入り・APIキー）
    ui.js            # DOM操作ヘルパー
README.md
.gitignore
```

`app.js` は薄いコントローラに留め、ロジックは `lib/` に分離する。ESM (`<script type="module">`) を使ってよい。

## 8. ガードレール（Claudeへの指示）

- 既存の `web/` `docs/` `web_v2/` `server/` `doc/` は **読まない・参照しない**。仕様はこのファイルのみ。
- 1ファイルが200行を超えそうなら分割を提案。
- 破壊的な変更（既存ファイル削除）は事前に確認。
- 秘密情報（APIキー実値）はコード・ドキュメントに書かない。
- コミットは小さく、機能単位で。

## 9. 完成判定

- [ ] APIキー未設定の状態で警告が出て検索ボタンが無効化される
- [ ] APIキー保存後、`ENEOS 丸の内` のような検索でヒットする
- [ ] 基本情報・地図・ストリートビューがすべて表示される
- [ ] 候補が複数の場合に候補一覧が表示される
- [ ] 比較枠に最大3件まで追加でき、× で個別に消せる
- [ ] 検索履歴がLocalStorageに保存され、再表示される
- [ ] お気に入りの追加・削除・一覧表示ができる
- [ ] スマホ表示でレイアウトが崩れない
- [ ] GitHub Pagesにデプロイして動作することを確認
